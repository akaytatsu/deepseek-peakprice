# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.*
*.local

# Editor / OS
.DS_Store

# Logs
npm-debug.log*

# TypeScript build info (generated inside node_modules)
*.tsbuildinfo

# NOTE: src/data/pricing.json is generated by scripts/fetch-pricing.mjs but is
# intentionally COMMITTED — it is the offline fallback when the DeepSeek site
# is unreachable, and the requirement states data must live in the source.

.claude/*
!.claude/skills/
.impeccable/critique/*
.impeccable/review/
